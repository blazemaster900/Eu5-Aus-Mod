REPLACE:GER_f = { 

	level = 4
	required_locations_fraction = 0.40 #reduced germany formable requirements to 40%
	rule = plausible
	potential = {
		culture = { has_culture_group = culture_group:german_group }
	}
	
	name = GER
	flag = GER
	adjective = GER_ADJ
	tag = GER
	color = map_GER
	
	regions = {
		north_german_region
		south_german_region
	}
	form_effect = {
		if = {
			limit = { country_rank_level < 4 }
			set_country_rank_effect = { rank = country_rank:rank_empire }
		}
	}
}

SGF_f = { 

	level = 3
	required_locations_fraction = 0.01
	rule = plausible
	potential = {
		culture = { has_culture_group = culture_group:german_group }
	}
	
	name = SGF
	flag = GER
	adjective = SGF_ADJ
	tag = SGF
	color = map_BAV
	
	regions = {
		south_german_region
	}
	form_effect = {

		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}

		add_country_modifier = {
			mode = add_and_extend
			modifier = germanic_expansion
 			years=-1 
 			recalculate_immediately = yes #forces game to update effects immediately
		}	

	}
}

NGF_f = { 

	level = 3
	required_locations_fraction = 0.01 
	rule = plausible
	potential = {
		culture = { has_culture_group = culture_group:german_group }
	}
	
	name = NGF
	flag = GER
	adjective = NGF_ADJ
	tag = NGF
	color = map_PRU
	
	regions = {
		north_german_region
	}
	form_effect = {

		if = {
			limit = { country_rank_level < 3 }
			set_country_rank_effect = { rank = country_rank:rank_kingdom }
		}

		add_country_modifier = {
 			modifier = germanic_expansion
 				years=-1 #negative values are permanent duration
 				mode = add_and_extend
 				recalculate_immediately = yes #forces game to update effects immediately
		}	



	}
}